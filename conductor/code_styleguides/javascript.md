# JavaScript & React Style Guide

## Component Guidelines
- Use functional React components with hooks.
- Keep JSX clean and modular.
- Use explicit prop names and default parameters.
- Preserve non-technical UX: zero LaTeX exposure in client UI.

## Backend Guidelines
- Use Express routes with structured error handling.
- Keep API keys exclusively in server `.env`.
- Provide graceful fallback modes if external APIs fail.
